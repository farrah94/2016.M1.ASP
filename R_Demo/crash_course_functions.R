AddTwoNumbers <- function( x, y ) {
  z <- x + y
  return(z)
}

LogBase <- function( x, base = 10 ) {
  z <- log(x) / log(base)
  return( z )
}

AddTwoNumbersWrong <- function( x, y ) {
  z <- x + yy
  return(z)
}